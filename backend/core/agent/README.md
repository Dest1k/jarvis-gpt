# Agent Core

Future runtime modules:

- planner: converts intent into executable plans
- executor: runs validated actions
- memory: maintains context
- reflection: evaluates results

The agent remains model-agnostic through the LLM router.
