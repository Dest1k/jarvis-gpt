# Assurance foundation bootstrap summary (historical blocked candidate)

Bootstrap candidate state: `ASSURANCE_FOUNDATION_CANDIDATE_FOR_REVIEW`

Subsequent independent verdict: `FOUNDATION_REVIEW_BLOCKED` (`B01`–`B14`)

This file records the original bootstrap and is not a readiness attestation.
Current remediation status is recorded only under the dedicated remediation
review deliverables.

Recorded: `2026-07-13T20:33:44Z`

## Fixed context

| Role | Value |
| --- | --- |
| Run ID | `20260713T002206Z_686424795712` |
| Main checkout | `D:\jarvis-gpt` |
| Foundation worktree | `D:\jarvis-gpt-worktrees\assurance-20260713T002206Z_686424795712` |
| Foundation branch | `foundation/assurance-upstream/20260713T002206Z_686424795712` |
| Foundation base / orchestration HEAD | `b2c481de1a9e68079a67ff49790eb685a09e80e5` |
| Functional audit completion | `5aae9855f0779c746ec9287c2ec8917637fedb36` |
| Production source exercised | `3fda655e4f723a0d8f58a4edfb4b3ee7dda079fe` |

The three SHAs above retain separate meanings.

## Safety checkpoint

- Annotated tag:
  `pre-assurance-bootstrap-20260713T002206Z_686424795712`, peeled to the exact
  foundation base.
- Verified bundle:
  `D:\jarvis\audit-backups\20260713T002206Z_686424795712\assurance-bootstrap\pre-assurance-bootstrap-20260713T002206Z_686424795712.bundle`.
- Bundle SHA-256:
  `ab31161a3aee6372a2432c0531c6c3ce48c9a8a25dd027c7270f7595e572dcb2`.
- The legacy main-checkout untracked baseline contains 269 paths, all under
  `.audit/**` or `JARVIS_FULL_AUDIT_PROMPT_FIXED.md`. Its saved list SHA-256 is
  `443722c67fed1727728d3a0242d4bf4573549784a58536328a99c191e1f87fbc`.
- This value anchors only the sorted path list, not file type, size or bytes;
  it is insufficient for wave execution. Protocol `10` now requires external
  before/after content manifests with retained SHA-256 anchors. Initial tracked
  diff and index were clean.
- Functional state was verified from the committed tree:
  `COMPLETE_WITH_BLOCKERS`, progress 100, no `functional/READY`, present
  `functional/spark/READY`, 17 queue rows, 17 Spark task files, and zero
  `NOT_RUN` queue rows.

## Delivered foundation

- Permanent `qa/` harness with safe offline/loopback/CLI execution,
  append-only sanitized evidence, deterministic validators, replay, schemas,
  suites, and calibration fixtures.
- Typed review packets, six explicit independence levels, separate immutable
  review outputs, and adjudication that preserves disagreement and cannot
  override deterministic failure.
- Fail-closed offline upstream candidate/provenance gate and engineering
  policy. The donor registry is empty; both `_surfer` components are recorded
  as `commissioned_internal` with no external code imported.
- Remediation protocol `docs/audit/10_JARVIS_FUNCTIONAL_REMEDIATION_WAVES_PROMPT.md`.
  The blocked candidate version was insufficient. The remediation version
  additionally binds each wave to an exact reviewed input HEAD and requires
  external content manifests for the complete tracked/untracked `.audit` tree.
- Immutable remediation overlay with exact task order: Wave 0 (6), Wave 1 (4),
  Wave 2 (6), plus `SPARK-0013` behind separate `PROFILE-SAFETY` and
  `PROFILE-RESEARCH` decisions.

## Local commits

1. `573bd20b680b6dbc8201e303112e1b4c8e2630a0` —
   `assurance: add permanent deterministic QA harness`
2. `924bef7caf9a328a3353f8a7380cc986b744aa01` —
   `assurance: add independent review and adjudication contracts`
3. `66633f2d2e7192abd2dd9f9662bea4f76dfef451` —
   `upstream: add provenance/adoption gate and remediation-wave protocol`
4. The branch-tip commit closes the independent-review artifact and typed-replay
   gaps and refreshes this handoff. Resolve its exact SHA with
   `git rev-parse HEAD`; a commit cannot embed its own object ID.

## Historical bootstrap validation

- `py -3.11 -m pytest qa\tests -q`: 44 passed total.
  - deterministic core: 26 passed;
  - review/CLI: 5 passed;
  - upstream gate: 13 passed.
- `py -3.11 -m ruff check qa`: pass.
- `py -3.11 -m compileall -q qa`: pass.
- `validate-suite`: 1 operator-core scenario, pass.
- `validate-evidence`: 8 sanitized calibration records, pass.
- `replay`: 8/8 verdicts reproduced, zero mismatches; 1 PASS, 6 FAIL,
  1 INCONCLUSIVE.
- `build-review-packets`: 8 separate packets.
- Two separate semantic reviews preserved disagreement as `INCONCLUSIVE`.
- Two semantic `PASS` reviews of a deterministic failure adjudicated to
  authoritative `FAIL`.
- Upstream positive/negative fixtures cover missing commit, unknown license,
  missing finding/gap, copied code without source/provenance, `idea_only`, and
  commissioned-internal behavior.
- Four immutable source pins use `sha256_git_blob_raw_bytes_v1` and are
  recomputed from the exact local Git object database at commit
  `5aae9855f0779c746ec9287c2ec8917637fedb36`; each pin records path, Git blob
  OID and SHA-256 of raw blob bytes. Checkout bytes and EOL conversion are not
  authoritative. The verifier also proves exact 17/17 queue/index/task-blob
  mapping, with `SPARK-0013 / FUNC-FIND-013` outside numbered waves.
- The branch-tip follow-up closed two earlier review gaps, but the subsequent
  full independent review still returned `FOUNDATION_REVIEW_BLOCKED` with
  confirmed blockers `B01`–`B14`. Historical `44 passed` results therefore do
  not authorize wave execution.

All checks are offline and require no running JARVIS, Docker, model, external
network, or real credential.

## Scope and next gate

No `.audit/**`, `backend/src/**`, `frontend/app/**`, production script,
Docker configuration, or `D:\jarvis` runtime state was changed. The existing
Spark queue remains byte-identical. `docs/assistant-notes.md` is the only
coordination metadata outside the protocol deliverable roots, as required by
repository instructions. No dependency was installed and no push or merge was
performed.

Protocol `10` and the foundation remain blocked for wave execution until the
separate remediation candidate is completed, independently reviewed, and its
exact attested commit is supplied as both the reviewed foundation and exact
`REVIEWED_INPUT_COMMIT` for `WAVE-0`. No product READY marker exists or is
authorized by this foundation.
