# Candidate records

Store one JSON record per proposed upstream adoption. Records must conform to
`../CANDIDATE_SCHEMA.json`, reference only committed sanitized evidence, and be
validated offline before a human decision. This directory intentionally
contains no pre-approved external candidates.
